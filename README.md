# Euronext_Surveillance

The ``interfaces-integrations`` branch allows us to implement the project's interfaces and integration mechanisms, combining a classic RESTful API with internal communication via gRPC.

- API Gateway with rate limiting
- gRPC for internal communication

